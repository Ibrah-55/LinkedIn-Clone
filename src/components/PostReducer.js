export const initialState = {
    loading:false,
};
const PostReducer = (state = initialState, action) => {
    switch(action.type){
        default:
        return state
}
};

export default PostReducer;
